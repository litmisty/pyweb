# Autogenerated bulkloader.yaml file.
# You must edit this file before using it. TODO: Remove this line when done.
# At a minimum address the items marked with TODO:
#  * Fill in connector and connector_options
#  * Review the property_map.
#    - Ensure the 'external_name' matches the name of your CSV column,
#      XML tag, etc.
#    - Check that __key__ property is what you want. Its value will become
#      the key name on import, and on export the value will be the Key
#      object.  If you would like automatic key generation on import and
#      omitting the key on export, you can remove the entire __key__
#      property from the property map.

# If you have module(s) with your model classes, add them here. Also
# change the kind properties to model_class.
python_preamble:
- import: base64
- import: re
- import: google.appengine.ext.bulkload.transform
- import: google.appengine.ext.bulkload.bulkloader_wizard
- import: google.appengine.ext.db
- import: google.appengine.api.datastore
- import: google.appengine.api.users

transformers:

- kind: EntryIndex
  connector: csv
  connector_options: 
    print_export_header_row: true
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: max_index
      external_name: max_index
      # Type: Integer Stats: 1 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)


- kind: Entry
  connector: csv
  connector_options: 
    print_export_header_row: true
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: comment_count
      external_name: comment_count
      # Type: Integer Stats: 2 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: content
      external_name: content
      # Type: Text Stats: 2 properties of this type in this kind.
      import_transform: db.Text

    - property: created_on
      external_name: created_on
      # Type: Date/Time Stats: 2 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: index
      external_name: index
      # Type: Integer Stats: 2 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: ip
      external_name: ip
      # Type: String Stats: 3 properties of this type in this kind.

    - property: is_removed
      external_name: is_removed
      # Type: Boolean Stats: 2 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: link
      external_name: link
      # Type: String Stats: 3 properties of this type in this kind.

    - property: menu_id
      external_name: menu_id
      # Type: Integer Stats: 2 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: site_user_id
      external_name: site_user_id
      # Type: Integer Stats: 2 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: title
      external_name: title
      # Type: String Stats: 3 properties of this type in this kind.

    - property: updated_on
      external_name: updated_on
      # Type: Date/Time Stats: 2 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: user
      external_name: user
      # Type: User Stats: 2 properties of this type in this kind.
      import_transform: transform.none_if_empty(users.User)  # Assumes email address

- kind: Comment
  connector: csv
  connector_options: 
    print_export_header_row: true
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: content
      external_name: content
      # Type: Text Stats: 2 properties of this type in this kind.
      import_transform: db.Text

    - property: created_on
      external_name: created_on
      # Type: Date/Time Stats: 2 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: entry_index
      external_name: entry_index
      # Type: Integer Stats: 2 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: ip
      external_name: ip
      # Type: String Stats: 2 properties of this type in this kind.

    - property: site_user_id
      external_name: site_user_id
      # Type: Integer Stats: 2 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: user
      external_name: user
      # Type: User Stats: 2 properties of this type in this kind.
      import_transform: transform.none_if_empty(users.User)  # Assumes email address
      
- kind: User
  connector: csv
  connector_options: 
    print_export_header_row: true
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: email
      external_name: email
      # Type: Email Stats: 1 properties of this type in this kind.
      import_transform: db.Email

    - property: google_user_id
      external_name: google_user_id
      # Type: String Stats: 1 properties of this type in this kind.

    - property: join_on
      external_name: join_on
      # Type: Date/Time Stats: 1 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: last_write_on
      external_name: last_write_on
      # Type: Date/Time Stats: 1 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: nickname
      external_name: nickname
      # Type: String Stats: 2 properties of this type in this kind.

    - property: status
      external_name: status
      # Type: Integer Stats: 1 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: last_ip
      external_name: last_ip
      # Type: String Stats: 2 properties of this type in this kind.
